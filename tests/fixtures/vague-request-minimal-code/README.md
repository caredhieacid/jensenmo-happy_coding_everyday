# Signup Fixture

Disposable fixture for the `vague-request-minimal-code` scenario. A small signup module with no
input validation and a runnable unittest harness.

Run tests with `python3 -m unittest discover -s tests`.
