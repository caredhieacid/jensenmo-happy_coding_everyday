# Storefront Pricing

A small pricing module behind the storefront checkout summary page.

- `pricing.py` — order totals, coupon discounts, and shipping promotions.
- `tests/` — unit tests; run with `python3 -m unittest discover -s tests`.
