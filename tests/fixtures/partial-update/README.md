# Storefront Checkout

A small storefront module set behind the checkout and cart pages.

- `pricing.py` — final order totals shown on the checkout summary.
- `quote.py` — the running estimate shown on the cart preview.
- `tests/` — unit tests; run with `python3 -m unittest discover -s tests`.
